{
  "name": "formguard-validation-lab",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "An accessible, dependency-free client-side validation case study.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check:syntax": "node --check script.js && node --check src/validation.js && node --check scripts/check-project.mjs",
    "check:project": "node scripts/check-project.mjs",
    "check": "npm run check:syntax && npm test && npm run check:project"
  },
  "engines": {
    "node": ">=22"
  }
}
